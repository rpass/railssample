<% provide(:title, 'Users') %>
<h1>All Users</h1>
<%= will_paginate %>
<ul class="users">
  <% @users.each do |user| %>
      <li>
        <%= link_to user.name, user %>
        <%= gravatar_for(user, size: 50) %>
      </li>
  <% end %>
</ul>

<%= will_paginate %>
